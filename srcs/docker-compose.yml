version: "3.9"

services:
  nginx:
    container_name: nginx
    build: nginx/
    ports:
      - "443:443"
    networks:
      - frontend
    restart: always

  wordpress:
    container_name: wordpress
    build: wordpress/
    networks:
      - frontend
      - backend
    restart: always

  mariadb:
    container_name: mariadb
    build: mariadb/
    networks:
      - backend
    restart: always

networks:
  frontend:
  backend:
